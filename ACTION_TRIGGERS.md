# Action Triggers

第十九轮把情景决策矩阵中的稳健动作转成可执行的观察触发器。

## 第一性原理

一句“建议关注”不能直接驱动业务动作。可执行建议至少需要：

1. 起始条件
2. 升级条件
3. 降级条件
4. 停止条件
5. 责任角色
6. 自动化边界

因此 `action_triggers.json` 只生成 **advisory-only** 触发器，不自动执行承保、投资、合规、理赔或经营动作。

## 状态

当前统一从 `monitor` 开始。后续可以在真实用户反馈和运营数据基础上增加 `prepare` / `escalate` / `close`，但状态变化必须由新增证据触发，不能由时间自动升级。

## 质量边界

只有跨至少两个情景的 `robust_actions` 才会进入触发器层；单一情景不会被包装成稳健行动。
